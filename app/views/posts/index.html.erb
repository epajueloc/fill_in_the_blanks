<h1>Display of all the posts</h1>

<% @posts.each do |post| %>
	<h1><%= post.bru_name %></h1>
	<p><%= link_to 'Show', post_path(post) %></p>
	<p><%= link_to 'Edit', edit_post_path(post) %></p>
	<p><%= link_to 'Destroy', post_path(post), method: :delete, data: {confirm: "Are you sure?"} %></p>

<% end %>